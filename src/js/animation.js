// import gsap from "gsap";

// export function animateObject(object) {
//     gsap.to(object.rotation, { duration: 2, x: Math.PI * 2, repeat: -1, ease: "power2.inOut" });
// }

console.log("Hello, Vite!");